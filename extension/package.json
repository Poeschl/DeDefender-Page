{
  "name": "DeDefender Extension",
  "private": true,
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "build:chrome": "vue-tsc --outDir dist/chrome && TARGET=chrome vite build",
    "build:firefox": "vue-tsc --outDir dist/firefox && TARGET=firefox vite build"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.1",
    "@vitejs/plugin-vue": "^4.3.1",
    "typescript": "^5.1.6",
    "vite": "^5.1.2",
    "vite-plugin-web-extension": "^4.1.1",
    "vue-tsc": "^1.8.8",
    "webextension-polyfill": "^0.10.0"
  }
}
